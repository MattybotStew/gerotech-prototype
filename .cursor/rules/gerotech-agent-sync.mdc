---
description: Cross-editor sync — Cursor, VS Code (Cline), shared session state
alwaysApply: true
---

# Gerotech — multi-agent sync (Cursor + Cline)

This repo is edited in **Cursor** and **VS Code (Cline)**. Stay aligned via git + shared files.

## Every session — read first

1. `.clinerules` — live session state, blockers, recent decisions
2. `JOURNAL.md` — newest entry first
3. `git log -5 --oneline`

Also useful: `AGENTS.md`, `design-spec.md`, `docs/PROJECT_BRIEF.md`, `cline-project-handoff.md`

## Every session — write before ending

1. Prepend a short entry to `JOURNAL.md` (date, agent, what changed, loose ends)
2. Update **Current Session State** in `.clinerules` (last updated, active agent, last commit)

## Code conventions (do not drift)

- Static HTML/CSS/JS only — no frameworks or build tools
- CSS order: `tokens.css` → `components.css` → `layout.css` → **`elevated.css`**
- Navigo font kit `lqh7ybe` — **400 + 700** in kit; tokens ready for **500** when Matt adds Medium
- BEM classes; brand colors only in `tokens.css`
- Unsplash placeholders OK (comment each img); verify URLs — some 404 over time
- Hero + CTA photo bands: left-aligned, left gradient overlay (matches homepage)
- Interior heroes: all sub-pages **`.page-hero`** (homepage slide pattern); per-page `<img class="slide__bg">` in HTML
- Never guess open client decisions
- **WP Engine Dev:** theme-only push to `gerotechdev`; never Production; never Local DB. F1 image `haas-f1-team.jpg`. Read `.clinerules` pickup block.

## Design directions (2026-08-17 — do not drift)

- **Accent system:** `.accent` / `.accent--deep` in headlines; `.headline-rule` under section titles (48px orange bar)
- **Haas Relationship:** watermark in `.haas-relationship__bg` (static, intro only). Current homepage (`7306:1063`) has **no** 4-col features band — do not re-add. Features CSS (`7080:2240`) may remain unused on home.
- **News (homepage):** `.news-editorial` split — photo lead `.news-feature` + numbered `.news-item` rows; keep `.news-card` on ES/showroom
- **Testimonials:** `partials/testimonials-block.html` → `.testimonial-grid` + refreshed `.testimonial-card` (orange top rule, quote watermark, Barlow name); not carousel
- **Display type:** Barlow Condensed for headlines/card titles/numerals; Navigo for body/UI
- **Feature icon tiles:** 40×40 `--clr-gray-card` square (4px radius), 18px black/ink stroke icons — shared by `.haas-relationship__icon`, `.category-card__icon`, `.why-feature__icon`
- **Figma nodes:** Homepage handoff **`7306:1063`** (canvas `6573:406`); Haas intro `7080:1405`; features `7080:2240` not on current home. Full detail in `AGENTS.md`.

## Local preview

Run **Tasks: Run Task → Serve Gerotech (8080)** or `python3 -m http.server 8080` from repo root.

If connection refused or empty response: `kill $(lsof -t -iTCP:8080 -sTCP:LISTEN)` then restart.

## Figma

Paste a frame/layer link with `node-id`. Use `get_design_context`, then adapt to this project's HTML/BEM/tokens — not React/Tailwind verbatim.
