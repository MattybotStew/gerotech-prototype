# Gerotech Prototype — Shared Agent Rules
# Read this before touching any file. Update after every change.

---

## Current Session State

**Last Updated:** 2026-07-06
**Active Agent:** Claude Code
**Last Commit:** f967ce1 — feat: restructure ES services to 3 categories per content doc

### Figma Comments — ALL DONE
All 17 Figma comments from the client review have been implemented:
- Partners section removed from homepage
- "Take Action Now" eyebrow removed from all CTA bands
- Testimonial carousel (3 slides, dot nav) on homepage + ES page
- Stat strip redesigned: numbers in boxes
- Section breathing room added
- ES grid restructured to 3 categories: Machine Customization (9), Applications (6), Automation and Controls Solutions (6)
- Category names updated site-wide per content doc (PDF from CloudMellow handoff)
- Detail pages updated: machine-custom-solutions, application, automation-integration heroes + card grids
- Mega-nav updated with new category names and service lists

### Blocked / Open Decisions
1. Hero slides (#57) — content doc names 3 categories but no hero copy/images provided yet
2. "Remove T" (#56) — unclear what T refers to; needs client clarification
3. Highlight key info in slider (#45) — which info to highlight not specified
4. Remove 3 homepage sections + Haas showcase (#50) — which 3 sections not confirmed
5. CTA destination (#48) — pending client decision
6. Wireframe sign-off from Rochelle/CEO — confirm status
7. Training: own nav item vs. under Support dropdown
8. FANUC badge usage rights — placeholder only
9. Stat strip numbers — need real numbers from client

---

## Who else is working on this codebase

**Cline** and **Claude Code** both work on this codebase. Keep this file up-to-date so the other agent always knows current state before touching anything.

---

## Project Context

- **Client:** Gerotech — Michigan CNC machinery distributor, Haas Factory Outlet, est. 1987
- **Agency:** CloudMellow (Matt)
- **This repo:** Static HTML/CSS/JS prototype (NOT the production build — final site will be WordPress)
- **Post-presentation next step:** Incorporate Rochelle's feedback, then proceed toward design phase

---

## Tech Stack

- Pure HTML5 / CSS3 (custom properties) / vanilla JS (ES6) — zero frameworks
- Font: Plus Jakarta Sans (400/500/600/700/800) via Google Fonts
- CSS load order: `assets/css/tokens.css` → `assets/css/components.css` → `assets/css/layout.css`
- JS: `nav.js` (both pages), `slider.js` (homepage only), `filter.js` (ES page only), `animations.js` (both)

## Key Brand Tokens
- Orange: `#F38A2C` (`--clr-orange`)
- Dark bg: `#1C1C21` (`--clr-dark-bg`)
- Dark nav/footer: `#3A3A40` (`--clr-dark-nav`)
- Max-width: 1200px (homepage) / 1120px (ES page)
- Radii: buttons 8px, cards 18–20px, pills 99px

---

## What NOT To Do

- Never replace `<div class="img-placeholder">` with real images until assets are confirmed
- Never guess at open client decisions — use placeholder + HTML comment
- Never use real FANUC logo — badge usage rights unconfirmed
- Never change CSS load order
- Never hardcode brand colors outside `tokens.css`
