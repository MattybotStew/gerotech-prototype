# Gerotech Prototype — Shared Agent Rules
# Read this before touching any file. Update after every change.

---

## Current Session State
**Last Updated:** 2026-09-22 (Claude — remaining hardcoded copy is now ACF-editable; MCS hero H1 matches the homepage)
**Active Agent:** Claude
**Branch:** `master`
**Last Commit:** `e3a3745` (ACF editability pass) — pushed; `origin/master` 0 ahead / 0 behind.
**Uncommitted:** docs only.
**Deployed:** all MCS-page image work plus this ACF pass are live on **Local + Dev**, both seeded. Post-deploy audit: **13 Local + 13 Dev pages 200 / 0 PHP warnings**, Dev theme in sync with the repo.

### ACF coverage — what is editable now (2026-09-22)
Every template reads through ACF. Newly added this pass:
- **MCS hero H1** — `mcs_hero_accent_color` (White/Haas Red/Brand Orange, **blank by default**) + `<em>` accent words in `mcs_hero_lead` / `mcs_hero_main`. This was the last hero without homepage functionality.
- **Service page** — 6 tab labels as *individual* fields (bound to fixed tab ids — never convert to a repeater), the planned-maintenance checklist as two repeaters (`heading` / `items` one-per-line / `column`), the optional-services block, the request heading+body, the Application Support note, and the office locations.
- **Careers** table headers, **Contact** page title + form heading, **Training**/**About** page titles.
- **Global** `Site Content → Forms` — mailing-list label, placeholder and button (previously duplicated across six templates).

Seeded by `scripts/seed-acf-content.php` (idempotent; never touches accent selects).

### Two things to know before editing these areas
- **Breadcrumbs must `strip_tags()` headline fields.** The MCS hero printed literal `<em>` tags in its breadcrumb because the headline is now accent-capable but the breadcrumb escaped it. `page-automated-system.php` had it right; copy that pattern.
- **The `#location_grand_rapids` / `#location_flat_rock` anchors are load-bearing** — `assets/css/legacy.css` targets them. They are now an explicit `anchor` field, *not* derived from the location name. Deriving them breaks the responsive rules silently.

### Removed dead fields
`mcs_hero_eyebrow`, `app_hero_eyebrow`, `ai_hero_eyebrow` were registered but rendered by nothing (all empty in the DB). Removed with notes. If a design ever gains a hero eyebrow on those pages, copy the ES hero field + markup rather than re-adding a dead field.

### MCS card artwork — source of truth
Cards take their photo from the **stored `mcs_cards` ACF rows**, not the template default array, so every image change needs a media import + `mcs_cards_<i>_image` update on Local *and* Dev. Current row order: 0 Column Risers, 1 Safety & Environmental, 2 Sheet Metal, 3 Auto Doors (video, row also has a `video` sub-field), 4 Hydraulic–Pneumatics, 5 Custom Workholding, 6 Process Engineering, 7 Specialty Machine.

**`custom-fixtures.jpg` and `custom-workholding.jpg` are deliberately different photos** — the former is now only the gallery collection "Custom Fixture Design" cover/lightbox, the latter is the service card. Don't collapse them.

### Portrait artwork in landscape cards
The MCS cards are 275px tall and landscape (~2.15), so `object-fit: cover` shows only a **~26% centred vertical band** of a portrait source. Preview the crop before assuming it works — `sips -c <h> <w>` reproduces it exactly (e.g. `sips -c 472 1012` for a 1012-wide asset).

### Figma access from this session
No Figma MCP is available here, but `~/.figma_token` exists — use the REST API directly: `/v1/files/:key/nodes?ids=<a:b>` to identify a node, read an IMAGE fill's `imageRef`, then `/v1/files/:key/images` to resolve the original. `/v1/images/:key?ids=…&format=png&scale=2` renders any node to a PNG for visual checking. File key `YgHwqyyFj57c1ZSbmfkL0c`.

### MCS service cards — `mcs_cards` repeater
The card grid is an ACF repeater (`title`, `image`, `video` (optional), `detail`). **The stored rows override the template's default array** — `$cards = $pick( 'mcs_cards', <default> )` — so adding a key to the PHP default changes nothing on Local/Dev until the row is seeded. `video` makes a card a muted inline looping thumbnail (`autoplay muted loop playsinline`, `image` as poster, `aria-hidden`); `animations.js` pauses it under `prefers-reduced-motion`. Only Auto Doors uses it so far; `auto-door.mp4` is 4.4MB and downloads with the page.

### Naming — SETTLED (2026-09-22)
**"Machine Custom Solutions"** is the canonical client-facing name (was open decision #3 in `cline-project-handoff.md`). Write it exactly that way. Do not reintroduce "Machine Customization" or "Machine Customizations".

- The "nav says Machine Customization" conflict was **never real** — it existed only as a false claim inside a placeholder comment, now deleted. All nav/menu/heading/search/ACF surfaces already agreed.
- The live slug stays **`/modification-of-standard-machine-tools/`** on purpose. Links resolve via one map in `inc/helpers.php` (`'machine-custom-solutions' => '/modification-of-standard-machine-tools/'`); `functions.php` 301s `/machine-modification` there. **Display name and URL are allowed to differ — do not "fix" the slug without a redirect plan.**
- The only residual: the client's own wording was "Machine Customizations" (plural). Shipped name differs; a rename would be display-text-only if they insist.
**Known non-blocker:** parent theme `gerotech/functions.php:13` ("Undefined variable $arguments") warns under WP-CLI only — **not** visible on any web page (confirmed on cache-busted URLs). Parent theme is not in this repo.

### Homepage hero colour selects — no ACF defaults (2026-09-22, commit `94332cd`)
`accent_color` / `cta_color` deliberately have **no `default_value`** (`''` + `allow_null` + `placeholder`). ACF **injects a field's `default_value` on read** when a repeater row has no stored value, so a default would silently override the positional design colours (slide 1 Haas red, rest Brand Orange) on any database that predates the field — and a naive wp-admin "Update" would then persist that wrong value. Blank now means "no explicit choice" and falls through to the retired `accent_class` meta, then to the design default. Same blank-default pattern is used by every interior `*_hero_accent_color` / `*_hero_cta_color` field. Re-seed with `scripts/seed-home-hero-colors.php` (idempotent; `wp eval-file`, needs to be copied to the server since `scripts/` is not part of a theme-only push).

### Latest Projects & News — PARKED, client-toggleable (2026-09-22)

Client: "we just don't know if we can support the Latest Projects & News right now… disable on all pages BUT keep it as a component that can be easily added back by the client."
- **Theme:** ES news markup extracted to **`template-parts/sections/news.php`**, rendered only when ACF toggle **`es_show_news`** (`field_es_show_news`, `true_false`, **default 0**) is on — the client can re-enable it from wp-admin (ES → News tab) with no dev work. Content seeded into ACF on 2026-09-18 was **left in the DB** (stored rows win; they reappear when toggled on).
- **Prototype:** `engineered-solutions.html` SECTION 12 is now only a pointer comment — the markup was **moved** (not duplicated) into `partials/news-block.html`, with a commented-out one-line `data-include` left in ES. Re-enable = uncomment it. (Earlier notes said the markup was "preserved verbatim inside the comment" — that was wrong; it was removed and relocated.)
- **Only ES had it** — Homepage news was already removed in the Figma-alignment pass. Re-enable: flip the toggle (WP) or unwrap the parked comment (prototype).



**Last Updated:** 2026-09-22 (opencode — partner logos are now a true ACF repeater; client can upload/swap/reorder in admin)
**Active Agent:** opencode
**Branch:** `master`
**Last Commit:** working — repeater change not yet committed.
**Uncommitted:** `page-engineered-solutions.php` + `inc/acf-fields.php` (partner logo ACF repeater).
**Deployed:** repeater change live on Dev + Local (fallback path — 12 mapped logos — renders; repeater currently empty on both). Repeater branch smoke-tested on Local with a seeded FANUC media-lib upload (tile + alt correct), then wiped.

### Partner logos = client-editable (ACF repeater)

- Field `es_partners_logos` (repeater: `image` ACF-image, `brand` alt/text, `url` optional link) registers on the **Engineering Driven Solutions** → Technology Partners tab, alongside the legacy `es_partners_wordmarks` textarea (kept as fallback).
- `page-engineered-solutions.php` renders: **repeater rows** when non-empty (each row = `.partner-wordmark` tile; optional `<a>` when link set; alt falls back to attachment alt, then 'Partner logo'); otherwise the **mapped wordmark list** (`$partners_logo_files`, drives `assets/images/tech-partners/*.jpg`).
- Client workflow: empty repeater = current 12-brand default. Adding rows overrides the defaults entirely — no dev needed to add/swap/remove/relink logos.
- Gotcha (recurring theme): **stored ACF rows win over template defaults** — same lesson as the wordmark list; if a client adds rows then deletes them, ensure the field is emptied (not left with stale empty rows).

### 2026-09-22 session (Cline — Applications: Fire Suppression + RFID removed from the BUILD, not just the prototype)

Client MSG (Tristien Bridges, Sep 21): those two prototype-only Applications sections must **not go to development**.

- **Prototype** was already clean (`application.html`, Kimi Work) — re-verified: 6 cards / 6 collections, 0 hits, HTTP 200.
- **The WP build was NOT.** `page-unique-applications-for-standard-machines.php` still shipped both as template *defaults*, **and** the 2026-09-18 seeding had materialised **8 cards + 8 collections into ACF on Local and Dev**, so both environments rendered them (6 `fire-suppression` + 6 `rfid` hits, HTTP 200). Fixing the template alone would NOT have fixed the live page — **stored rows win over code defaults**.
- **Fixed:** removed the 2 cards + 2 collections from the template defaults, and added idempotent **`scripts/remove-apps-fire-suppression-rfid.php`** which deletes those rows by title. Ran on Local (8 → 6 in both repeaters) and on Dev (8 → 6); a second run is a **no-op** ("already clean"), proving idempotency.
- **Verified after cache flushes:** prototype, Local and Dev all render **6 cards + 6 gallery collections, 0 fire-suppression, 0 rfid**, HTTP 200, no PHP errors. Dev theme `rsync --checksum` dry-run = **zero drift**.
- **MCS deliberately untouched** — the client scoped this to APPLICATIONS; `machine-custom-solutions.html` and its template keep their own Fire Suppression collection.
- **No `wp` CLI on this machine — Local DB work recipe:** Local's bundled PHP + a `wp-load.php` bootstrap, no wp-cli needed:
  `~/Library/Application Support/Local/lightning-services/php-8.2.29+0/bin/darwin-arm64/bin/php -c "$(ls -d ~/Library/Application\ Support/Local/run/*/conf/php/php.ini | head -1)" /tmp/run-local.php scripts/<script>.php` (that php.ini already sets `mysqli.default_socket`). Dev is unchanged: `wp eval-file -` over SSH stdin.
- ⚠️ **Dev is ahead of git:** the other agent's uncommitted copy change rode along with this session's `rsync` (a theme push sends the whole working tree). It renders correctly (header 10× new label; home / ES / MCS / Automation all 200), but it must be committed or Dev ≠ `master`.

**Hero image (2026-09-21, DSH) — `hero-campus-vans.jpg` is retired.** Slide 1 is now **`assets/images/hero-slide-1.jpg`** (1920×1279, 619 KB) with **`hero-slide-1@2x.jpg`** (2560×1706, ~1.0 MB) offered through `srcset` + `sizes="100vw"`. It replaces `hero-campus-vans.jpg` (1024 px → blurry when full-bleed, which is why Ken Burns upscaling looked bad) and `newHeroSlide.jpg` (the 7.2 MB / 4096 px raw drop, only ever referenced by the prototype). New helper **`gerotech_image_srcset()`** (`inc/helpers.php`) builds the theme-asset `@2x` pair, or defers to `wp_get_attachment_image_srcset()` when the field holds an attachment. Same treatment for the ES hero: **`es-hero.png` (2.8 MB) → `es-hero.jpg` (500 KB)**. Local media: homepage hero = attachment **3467**, ES hero = **3468**; `hero-campus-vans.jpg` (3466) and `es-hero.png` (3429) are now **orphaned attachments** (upload files still on disk — safe to delete whenever).

**Peek accent (2026-09-21, DSH):** the feature that only existed in the Local theme is now back-ported to the prototype (source of truth): `slider.js` reads `data-peek-accent` and wraps the word in `.hero-slider__peek-accent` (brand red), `components.css` carries the rule, `index.html` has `data-peek-accent="Haas"` on slide 1, `front-page.php` emits it, and the **missing ACF sub-field `peek_accent`** now exists (`group_home_content` → Hero slides). Without that field the value could never be saved — Local rendered `data-peek-accent=""` until it was added. Note the active slide's peek is intentionally not rendered (`renderPeeks()` skips `this.current`), so slide 1's red **Haas** only shows while slide 2/3 is active. **Ken Burns is off** (`.slide.is-active .slide__bg { animation: none }`) in proto + theme.
**Slide alt fix (2026-09-21, DSH):** `title_alt` is now checked with `! empty()`, so slides 2–3 fall back to their eyebrow instead of rendering `alt=""` (ACF repeats every sub-field, so an untouched alt is an empty string).

**Hero accent colour — client-editable (2026-09-21, DSH):** each hero slide has an **Accent colour** select in ACF → **White (default) / Haas Red / Brand Orange**. It colours the `<em>` accent word in the slide headline *and* the matching word on that slide's peek card (`data-peek-accent-color` → `.hero-slider__peek-accent--{white|haas|orange}`, whitelisted in `slider.js`). Field is `accent_color` on the `home_hero_slides` repeater → `field_home_hero_accent_color`; `gerotech_accent_choice()` normalises any value (including the retired raw classes `accent`, `accent--haas`, `accent--deep`) and `gerotech_accent_class()` maps it to classes. Current: slide 1 `haas`, slides 2–3 `orange`.

**Haas Relationship colours — client-editable (2026-09-21, DSH):** two more selects in the **Haas Relationship** tab, same White / Haas Red / Brand Orange palette, both defaulting to **Haas Red** (matches Figma): **`haas_eyebrow_color`** (colours the eyebrow text *and* its short rule together) and **`haas_accent_color`** (colours the `<em>` accent words in the "Proud to Be Michigan's / Haas Factory Outlet" headline). The eyebrow uses one shared custom property — `.haas-relationship .eyebrow-row { --eyebrow-accent: … }` with `--white` / `--haas` / `--orange` modifiers — so the text and rule can never drift, and **the unmodified default is Haas red**, so older markup still renders correctly. Seeded `haas`/`haas` on Local. Scoped to `.haas-relationship`; interior page eyebrows are untouched. The old `.haas-relationship__headline .accent--haas` override was removed (redundant now that `.accent--haas` stands alone).

**Hero CTA button colour — client-editable (2026-09-21, DSH):** each slide also has a **Button colour** select — **Brand Orange (default) / Haas Red / White outline** — independent of the accent colour. Field `cta_color` → `field_home_hero_cta_color`, mapped by `gerotech_btn_class()` (`btn--primary` / `btn--primary btn--haas` / `btn--outline-white`). Current: slide 1 `haas` (red), slides 2–3 `orange`. `--clr-haas-red` + new `--clr-haas-red-dark` tokens replace the hardcoded `#CF0A2C` / `#b00926` in `.btn--primary.btn--haas`.

- **⚠️ ACF injects `default_value` on read — now neutralised (2026-09-21, opencode).** A repeater row with no stored value returns the field default, *not* empty — so a field with a default silently changes the design the moment anyone saves. Worse, this **actually fired on Local during DSH testing**: opening Home in the editor (fields showed the old `white`/`orange` defaults) and clicking Update persisted `white` over all six fields, wiping slide 1's Haas red. Fixed properly rather than guarded: both selects now have **no default_value and `allow_null`**, so the editor shows blank ("Leave unset to keep the design colour") and a save persists nothing. `front-page.php` resolves blank → (1) retired `accent_class` meta read **raw** with `get_post_meta()` (it is unregistered, so absent from the ACF row), then (2) the **design treatment (slide 1 Haas red, rest orange)** — matching `scripts/seed-home-hero-colors.php`. Seeds are idempotent. A fresh env (Dev) renders the design correctly with no DB work.
- **Bug fixed with it:** the CSS rule was `.accent.accent--haas` (compound) while the theme emitted only `accent--haas`, so **Haas red never applied on WordPress** — it silently rendered white. Modifiers now stand alone and are declared after `.accent`; verified by computed style (orange `rgb(243,138,44)`, Haas `rgb(207,10,44)`, white `rgb(255,255,255)`; buttons orange-on-ink, red/white-on-white, white-outline).
- **Interior hero colours — client-editable (2026-09-21, opencode):** same blank-select pattern on the **4 pages whose hero carries an accent word** — ES (`es_hero_accent_color`), Applications (`app_hero_accent_color`), Automation (`ai_hero_accent_color`), Careers (`careers_hero_accent_color`) — White / Haas Red / Brand Orange, blank keeps **Brand Orange** (the design) via the template (`$pick( '<prefix>_hero_accent_color', 'orange' )` → `gerotech_accent_class()`). Plus hero **button colour** where the hero has a CTA: ES primary (`es_hero_cta_color`) and Careers (`careers_hero_cta_color`), blank keeps Brand Orange. **Not rolled out (by design):** MCS hero has *no* accent word (`mcs-name-split` plain lead/main) and no hero CTA; Training / Support / About use **legacy markup** (no `.page-hero`/accent spans) — neither has anything to colour. ES's secondary button stays fixed `btn--outline-white`. No DB migration needed (blank = design). Verified end-to-end on Local: blank renders `.accent` + `btn--primary` (:run: unchanged), stored `haas` renders `accent--haas` + `btn--primary btn--haas`, reverted clean.
- **Still hardcoded:** interior **section eyebrows** (`.eyebrow` across all pages) and section-header accents stay fixed; the interior fields cover hero accents + hero CTAs only.

**Homepage Figma comments (2026-09-21):** Unresolved pins live on older frames `7196:2420` / deleted `7283:*` / `7306:520` — **not** on handoff `7306:1063` (zero comments). Applied: slide-1 van/F1-sign photo (now `hero-slide-1.jpg`, see above); stats band centered (`.stat-counter__grid--2`); mill UMC-750 `object-position: center 18%`; CTA phone lockup restored. **Do not remove mailing list** (#203). Remaining: other attached photos (149, 165, 166, 179, 191, 237, 242) — Figma REST does not return attachments.

**Dev deploy (2026-09-21, opencode — DONE):** pushed theme + seed + hero art; caches flushed. See the **Pushed to Dev** section below for the working recipe and gotchas. The earlier 11:07 ET hang did not repeat — gateway returned a shell this time.

**Dev theme verified (2026-09-18):** checksum rsync of `wp-content/themes/gerotech-child/` repo → `gerotechdev` = **zero content drift**. Direct SSH deploy works when the gateway is responsive (key `~/Library/Application Support/Local/ssh/wpe-connect`, user `gerotechdev@gerotechdev.ssh.wpengine.net`, path `/nas/content/live/gerotechdev/wp-content/themes/gerotech-child/`). WP-CLI + `wp page-cache flush` / `wp cdn-cache flush` available over the same SSH.

**Dev DB `_wp_page_template` fix (2026-09-18):** Dev's DB still had stale parent templates overriding our child templates — the 4 ES pages (`28 engineered-solutions`, `1250 modification-of-standard-machine-tools`, `1265 unique-applications-for-standard-machines`, `1239 automated-system`) pointed at `page-solutions.php`, and `1535 support` at `page-service-home.php`, so they rendered the **parent** markup. Cleared those metas (plus `11 home`, `14 about`, `16 contact`, `39 training`) and set `18 careers` to `default`, mirroring the Local DB. If Dev is ever re-pulled/reset, re-apply. `service` (`page-service.php`), `rotary-repair` (`rotary-repair.php`), `planned-maintenance` (`preventive-maintenance.php`) intentionally keep metas whose filenames match our child templates.

### Next session — pickup here

**Live Dev:** https://gerotechdev.wpenginepowered.com/ — **Gerotech Child** is the active theme. Homepage matches Figma `7306:1063`. Haas F1 card uses `assets/images/haas-f1-team.jpg` (200). Stage holds a copy of Dev for rollback.

**Do not** push Local → Production. **Do not** push the Local database. Theme-only: `wp-content/themes/gerotech-child/` (Select files). Uncheck `wp-admin`, `wp-includes`, `uploads`. After push: purge WP Engine cache, hard-refresh. (Direct rsync deploy also works — see above.)

### Pushed to Dev — 2026-09-21 (opencode) ✅

**Live Dev now current:** theme in sync (checksum dry-run clean), homepage hero = `hero-slide-1@2x.jpg` attachment **3467** with full srcset (slide 1 repointed in DB), ES hero = **3468** `es-hero.jpg` (500 KB); old ES `es-hero.png` (3429) orphaned on Dev like Local. Colours seeded: slide 1 `haas`/`haas`, slides 2–3 `orange`/`orange`, `haas_eyebrow_color=haas`, `haas_accent_color=haas`; interior hero fields left blank (design orange). Caches flushed (`page-cache`, `cdn-cache`, object `cache`). Verified over HTTP: all 14 URLs 200, 0 broken images (uploads + theme assets), slide 1 renders `accent accent--haas` + `btn btn--primary btn--haas`, new hero/ES art live.

**Working direct-deploy recipe (gateway was responsive this session):**
```bash
KEY="$HOME/Library/Application Support/Local/ssh/wpe-connect"
HOST="gerotechdev@gerotechdev.ssh.wpengine.net"
NAS="/nas/content/live/gerotechdev"

# 1. theme (rsync over ssh works; scp/sftp does NOT — "subsystem request failed")
rsync -avz --delete -e "ssh -i \"$KEY\" -o BatchMode=yes -o ServerAliveInterval=10" wp-content/themes/gerotech-child/ "$HOST:$NAS/wp-content/themes/gerotech-child/"

# 2. seed colours: WPE's ~/home is EPHEMERAL per-container (a file written via
#    one ssh dies on the next), so don't scp scripts — pipe stdin:
ssh -i "$KEY" "$HOST" "cd $NAS && wp eval-file -" < /path/to/seed.php

# 3. hero art: media_handle_sideload the pushed theme asset, set the field
#    (see dev-hero-import.php style — also works for ES). Then:
ssh -i "$KEY" "$HOST" "cd $NAS && wp page-cache flush && wp cdn-cache flush"
```
Gotchas: `wp eval-file -` reads PHP from stdin (boots WP, so theme helpers like `gerotech_image_srcset()` exist). `~` for a *short-command* ssh also resolves `/home/wpe-user` (ephemeral) — always use stdin or `$NAS` paths. `wp` emits harmless parent-theme `$feature` notices at CLI — ignore (source: gerotech `functions.php`, not child). Page slugs (AS of 09-18): ES `engineered-solutions`, MCS `modification-of-standard-machine-tools`, Apps `unique-applications-for-standard-machines`, Automation `automated-system` (not the goose-chase slugs `machine-custom-solutions` / `automation-integration` → 404).

**`scripts/seed-home-hero-colors.php`** (repo, committed) is idempotent and safe on any environment: stored choice wins → retired `accent_class` meta → original treatment. Verified by simulating a theme-only deploy on Local.

**Still unmerged:** `origin/claude/file-reading-19koen` holds 1 commit from 2026-07-06 that master has since overtaken by 153 commits — stale, do not merge. `origin/design-audit-revision` is already contained in master.

**Open leftovers (not blockers):**
1. reCAPTCHA “Invalid domain for site key” on Dev — **needs Google admin** (no API/credential here). Add `gerotechdev.wpenginepowered.com` to the Google reCAPTCHA admin allowed-domains for the CF7 site key `6LcOOuoaAAAAADRxx65d0pb_BvgWG9d9e5aqE9k6` (stored in the `wpcf7` option `recaptcha`). Ignore for visual QA.
2. **Unconverted Dev URLs need design/content direction** (no prototype): `machines`, `news-and-events`, and the leftover ES child slugs (`custom-workholding-and-tooling`, `engineering-process-optimization`, `hydraulic-pneumatic-clamping-systems`, `5-axis-cnc-grinders`, `integration-partners`, `custom-training-program`, `application-support`, `scheduled-training`, `parts-order`, `submit-a-request`, `preventive-maintenance-plan`, …). Do **not** invent these — they render with parent templates + our header/footer until scoped.
3. Local admin password may not match `localpass123` after a WPE Pull.
4. Adobe Fonts kit `lqh7ybe` is domain-locked — add production domain before launch.

**Verify pickup:** `git pull` · `./scripts/sync-theme-assets.sh --check` · `./scripts/sync-theme-to-local.sh --check` · open Dev homepage + Haas F1 card.

**Legacy (dev/live-body) pages:** `support`, `service`, `training`, `rotary-repair`, `planned-maintenance`, `about`, `contact` — body ported from dev/live + our header/footer, parent CSS scoped to `.legacy` (`assets/css/legacy.css`), Replica fonts from parent `fonts.css`. Enqueue list in `inc/enqueue.php`. (Contact ported from **live** because live carries the real `@gerotech.com` emails.)
- **Maps (2026-09-18):** the dev Google Maps API key (`AIzaSyCkwtPM8…`) is **deleted** — it threw `DeletedApiProjectMapError` and left gray boxes. Replaced the JS `initMap` + API script on `training` + `contact` with **keyless `<iframe>` embeds** (`https://www.google.com/maps?q=<address>&output=embed`, from the ACF locations repeater). No key needed; the `gerotech_google_maps_key` filter is no longer used.
- **ACF (Phase 2, 2026-09-18):** all 7 legacy pages are now ACF-driven via `inc/acf-legacy-fields.php` (registered by `post_name`) with the current markup as defaults — hero, intros, repeaters (service columns, courses, departments, locations), CTA. Forms (CF7 shortcodes), the PM inspection checklists, and map embeds stay hardcoded.
- **ACF `post_name` location rule (critical, 2026-09-18):** ACF has **no built-in `post_name` rule**, so every group located by `post_name == <slug>` (ES hub + MCS/Applications/Automation, all 7 legacy pages, Careers) silently **never matched** — the groups existed but never showed in the editor. Templates still rendered because of their code defaults, which masked it. Fixed by registering a custom rule (`acf/location/rule_types` + `rule_values/post_name` + `rule_match/post_name`) at the top of `inc/acf-fields.php`. If new groups are added, `post_name` now works.
- **Editor Meta Boxes:** in the block editor the ACF fields live in the collapsible **“Meta Boxes”** panel at the bottom (not the main canvas). `functions.php` `gerotech_hide_legacy_field_groups()` removes the **parent theme's** DB groups (Home Options, Page Options, ES/About/Training/Service Options, Rotary/Planned, Contact) on the 13 rebuilt pages so only our “— Content” group shows. Parent groups remain on unconverted pages/CPTs.
- **Content seeded into the DB (2026-09-18):** the fields are no longer empty — the templates' code defaults were materialised into ACF on **Local + Dev** (Homepage, ES, MCS, Applications, Automation, Careers, Training, Support, Service, Rotary, Planned, About, Contact + global testimonials). Image-field sources (theme assets + Unsplash stand-ins) were imported into the **media library** so image fields hold real attachment IDs. **Consequence:** the DB values now win over the code defaults — changing a template default no longer changes the front-end until the field is updated. Re-seed with the capture hook: set `$GLOBALS['gerotech_capture_defaults'] = array()` before rendering a template, then `update_field()` the captured defaults (see `inc/helpers.php` `gerotech_field()`).
- **`machine-modification` → MCS 301:** `functions.php` `gerotech_legacy_redirects()` (template_redirect on 404) redirects `/machine-modification/` → `/modification-of-standard-machine-tools/` (matches the prototype canonical redirect).
- **Careers (2026-09-18):** `page-careers.php` built from the `careers.html` prototype (new design system, `page-hero-trust`); ACF group `group_careers_content` in `inc/acf-fields.php`. Live at `/careers/`.
- **Template-name rule:** some pages have `_wp_page_template` pointing at a **parent** template (rotary-repair → `rotary-repair.php`, planned-maintenance → `preventive-maintenance.php`, service → `page-service.php`). Child templates override only a **same-named file** — match the parent's filename, not necessarily `page-{slug}.php`. Check with `wp post meta get <id> _wp_page_template`.

**Header nav:** still **hardcoded** in `header.php` (desktop `.site-nav` + mobile `.mobile-nav` — edit both). Support is a dropdown (`.has-dropdown` / `.dropdown`) with Service Request Forms / Rotary Repair / Planned Maintenance (no Training). Editable WP menus remain a retainer item — deferred until the IA locks.

### Prototype → WP build sync (read before editing shared assets)
The **prototype is the source of truth** for shared front-end assets until design lock; the WP child theme is a copy.

| Layer | Source | How it reaches the theme |
|---|---|---|
| CSS, JS, images | Prototype `assets/` | `./scripts/sync-theme-assets.sh` (one-way) |
| Markup (sections) | Prototype `*.html` | Port by hand into the matching PHP template — map in `handoff/theme-map.md` |
| WP-only pages (e.g. Contact) | Theme | Their CSS still lives in the prototype `components.css`, so the copy stays pure |

- After editing prototype CSS/JS/images: run `./scripts/sync-theme-assets.sh`. Use `--check` to see drift, `--prune` to drop orphaned theme images (it skips the theme-only `legacy/*` images).
- **Responsive imagery:** full-bleed heroes pass `srcset` + `sizes="100vw"`. Use `gerotech_image_srcset( $acf_value, $fallback_rel )` (`inc/helpers.php`) — it returns a **raw** value (escape with `esc_attr()`), prefers WordPress attachment sizes, and otherwise pairs the theme asset with its `@2x` sibling.
- **Large image guard:** heroes must not ship multi-MB files. `es-hero.png` (2.8 MB) and the 7.2 MB `newHeroSlide.jpg` were both replaced on 2026-09-21 — keep new hero art as JPEG/WebP at web resolution, not raw exports. `handoff/asset-manifest.md` + `handoff/qa-checklist.md` track this.
- `include-partials.js` and `gallery-module.js` are deliberately **not** synced (PHP includes replace the former; the latter isn't promoted).
- **At design lock:** tag the prototype, then the theme becomes source and syncing stops.

### Repo theme → Local site sync (PHP + everything else)
There are **two copies** of the child theme: the repo (`wp-content/themes/gerotech-child/`, source of truth, committed) and the running LocalWP site (`~/Local Sites/gerotech/app/public/wp-content/themes/gerotech-child/`).

- After editing the theme (PHP, CSS, images) **run `./scripts/sync-theme-to-local.sh`** — or `--check` for drift. Edits made only in the repo will NOT appear at `gerotech.local` until this runs.
- Full chain: prototype `assets/` → `sync-theme-assets.sh` → repo theme → `sync-theme-to-local.sh` → Local site.
- WP Engine "Pull" can overwrite the Local theme dir — re-run the sync after a pull.
- Local admin: `CloudMellow` / `localpass123` (reset via Local's bundled WP-CLI + MySQL socket if a pull overwrites users).

### Pickup notes (read this first)
- Static HTML/CSS/JS only. Haas red is `#CF0A2C` (Pantone 186 C) — use it on Haas word/CTA/Haas Relationship eyebrow, not as a global orange replacement.
- **Homepage Figma source:** canvas `6573:406` → home frame **`7306:1063`** (file `YgHwqyyFj57c1ZSbmfkL0c`). Align proto + WP homepage to this frame.
- Peek heroes: desktop `.hero-slider--peek` is **`height: auto; min-height: 500px`** at `min-width: 901px` (set in **both** `components.css` and `elevated.css` — elevated used to force `height: auto`). The hero **grows with the active slide's content** instead of clipping at a hard 500px (changed 2026-09-18 — previously fixed `height: 500px; overflow: hidden`). Short slides stay 500px; longer copy expands the hero (verified 500 → 767px with extra body copy). Padding **100px** top/bottom; copy vertically centered. Peek tiles **107px**. Peek CTAs not full-width; desktop peek buttons **+15px** `margin-top`.
- Peek labels: 02 **In-Stock & Ready**, 03 **Automation for Michigan**. Slide 2 **headline** stays “Our Showroom Machines Are Ready To Ship” (client line).
- Homepage CTA: Tristien body, photo **`assets/images/cta-home-figma.jpg`** (Figma `7306:1327`). **Phone lockup is back** (user reversed #241 / `7306:1063` no-lockup): `.cta-band--cinema-lockup` + `.cta-band__call` — Prefer to talk it through? / (734) 379-7788 / Talk to a person, not a form. `tel:+17343797788`. Keep the mailing list. Body **52ch** / copy max `--hero-copy-max`.
- Homepage hero slide 1: `assets/images/hero-slide-1.jpg` + `hero-slide-1@2x.jpg` (Gerotech/Haas vans + F1 Team sign) via `srcset` + `sizes="100vw"`; `gerotech_image_srcset()` builds the pair. `hero-campus-vans.jpg` / `newHeroSlide.jpg` are retired — do not re-add. Stats band `.stat-counter__grid--2` centered (max-width 720px). Mill lineup photo `.machine-panel__photo--umc img { object-position: center 18%; }`.
- Haas on homepage: watermark + F1 lockup + intro only. Brand card is `assets/images/haas-f1-team.jpg` (not `haas-f1-lockup.png` — that PNG 403s on WP Engine). No 4-column features band. Do not re-add without client/Figma direction.
- Interior `.page-hero` / `.page-hero-trust` share the 500px min-height + 100px padding.
- Proto header: Training / Support / About link to `training.html` / `support.html` / `about.html`. **WP Support stays a dropdown** (Service Request / Rotary Repair / Planned Maintenance).
- Testimonials: Ford + Kingbury quotes live in `partials/testimonials-block.html` (`?v=20260916`, `cache: 'no-store'`); WP reads global ACF options with those as defaults.
- Do not invent lineup series beyond the six mill chips. Other lineup tabs came from Matt/Tristien paste, not guessed.

### 2026-09-18 session (Cursor — child theme live on gerotechdev)
- Theme-only Local → **Dev** (`gerotechdev`): `gerotech-child` only, no DB. Activated **Gerotech Child** in wp-admin. Homepage live.
- F1 PNG 403 → `haas-f1-team.jpg` + `front-page.php` pushed; lockup OK. reCAPTCHA domain chip leftover.
- Git `077b5d0` + `ba259ca` on `origin/master`. Stage is Dev rollback.

### 2026-09-18 session (Cursor — homepage Figma alignment)
- Compared `gerotech.local` + proto to Figma `7306:1063`. Then aligned: peek titles, 500px hero, CTA JPEG, proto nav hrefs. WP Support dropdown kept. Recaptcha localhost badge is environment-only.
- Agent docs updated: `.clinerules`, `JOURNAL.md`, `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/gerotech-agent-sync.mdc`, `design-spec.md`, `cline-project-handoff.md`, `.github/copilot-instructions.md`.

### 2026-09-18 session (Bionic — testimonials → global ACF repeater)
- **`inc/acf-fields.php`:** added an ACF **options page** (`acf_add_options_page`, slug `gerotech-site-content`, title “Site Content”) + `group_site_testimonials` (location `options_page == gerotech-site-content`): `testimonials_eyebrow` / `testimonials_title` / `testimonials` repeater (`quote`/`name`/`sub`).
- **`template-parts/sections/testimonials.php`:** reads from `'option'` via `gerotech_field()` with v1 static quotes as defaults. Shared across homepage + 4 ES pages → edits once site-wide.
- **Decision (user-approved, Option A):** only testimonials made editable; icons / alt-text / aria-labels / breadcrumbs / email-form internals stay hardcoded.
- **Verified:** `php -l` clean; synced to Local; 5 URLs 200; fallback renders. Headless wp-admin re-verify still blocked (Local admin password).

### 2026-09-18 session (Bionic — ACF wired onto the 4 ES pages)
- **`inc/helpers.php`:** added `gerotech_field()` (null-safe ACF read with default) and `gerotech_parse_media()` (gallery-collection media list → `type|src|poster|alt|caption` items).
- **`inc/acf-fields.php`:** appended **4 field groups** (location `post_name == <slug>`, not `page_template`): `group_es_content` (`engineered-solutions`), `group_mcs_content` (`modification-of-standard-machine-tools`), `group_application_content` (`unique-applications-for-standard-machines`), `group_automation_content` (`automated-system`). Prefixes `es_*`, `mcs_*`, `app_*`, `ai_*`.
- **Wired 4 templates** (`page-engineered-solutions.php`, `page-modification-of-standard-machine-tools.php`, `page-unique-applications-for-standard-machines.php`, `page-automated-system.php`) to read ACF with the current static content as defaults (render correctly with empty fields).
- **Decisions:** MCS/Automation headlines use `lead` + `main` fields (`.mcs-name-split`); ES/Application use a single headline with `<em>` accent. Card modal detail is a `wysiwyg` field; the **“Talk to an Engineer” CTA is auto-appended** by the template. Gallery collections = repeater of `title`/`meta`/`media` (flat pipe-delimited textarea — no nested repeaters).
- **Verified:** `php -l` clean on all 6 edited files; synced to Local; all 4 URLs 200 with correct defaults (accent spans, card counts, gallery collections, `<details open>` preserved through `wp_kses_post`).
- **Deferred (user-approved):** legacy pages (Training, Support, Service, Rotary Repair, Planned Maintenance, About, Contact) — Phase 2.

### 2026-09-16 session (opencode — Phase 4 ACF: homepage wired)
- **`inc/acf-fields.php`** added (required in `functions.php`): Homepage group `group_home_content` (`page_type == front_page`) — Hero slides / Stats / Haas Relationship / Machine Lineup / CTA Band / Mailing List. Verified in editor.
- **`front-page.php`** now ACF-driven with current content as defaults (renders with empty fields; no regression).
- **Helpers:** `gerotech_accent()`, `gerotech_image_url()`, `gerotech_parse_tags()`.
- **Local admin:** post-pull users are dev's — use `CloudMellow` / `localpass123` locally.
- Next: ES hub, then remaining pages.

### 2026-09-16 session (opencode — Training/Support/About + Contact pages)
- **Converted** `page-training.php`, `page-support.php`, `page-about.php` (dev slugs; cleared their `_wp_page_template`). **Built `page-contact.php`** (no prototype — from dev's real content) + new `.contact-*` CSS in `components.css`.
- **Verified** all 9 pages 200 @ 360–1920, no overflow/errors. Site-wide reCAPTCHA badge (plugin) noted. Plan §12b → 9 pages. Next: Phase 4 ACF.

### 2026-09-16 session (opencode — nav links + ACF status)
- **Nav:** Training/Support/About linked (`/training/`, `/support/`, `/about/`); **Contact** added (`/contact/`) — desktop + mobile. `contact` added to `gerotech_page_url()`. All 4 pages 200.
- **ACF:** Pro active + parent field groups present, but our 5 templates have **no ACF calls** → client editing not wired yet (Phase 4). Nav pages render with parent markup + our CSS (unfinished, accepted).

### 2026-09-16 session (opencode — full dev pull + ES templates mapped to dev slugs)
- **Full pull** `gerotechdev` → local (`gerotech.local`, https): 30 pages, plugins (ACF Pro, Megamenu, Smart Slider 3, CF7, WPForms, iThemes Security…), media. Child re-copied + activated.
- **Mapping A:** our design takes over dev URLs. Renamed `page-machine-custom-solutions.php`→`page-modification-of-standard-machine-tools.php`, `page-application.php`→`page-unique-applications-for-standard-machines.php`, `page-automation-integration.php`→`page-automated-system.php`; `gerotech_page_url()` map → dev URLs.
- **Cleared** the 5 pages' parent `_wp_page_template` meta (was `page-solutions.php`) so slug templates apply.
- **Verified** 5 pages 200 @ 360–1920, no overflow/errors. **Push deferred.** Plan §12b.

### 2026-09-16 session (opencode — ES section pages built in WP)
- **Scope confirmed:** homepage + Engineered Solutions section = **5 pages** (not 11). Out: Training, Support, About, Careers, showroom, hero-variations, machine-modification.
- **New templates:** `page-engineered-solutions.php`, `page-machine-custom-solutions.php`, `page-application.php`, `page-automation-integration.php` (scripted conversion; `php -l` clean). Local DB pages created (slugs match), rewrites flushed.
- **Verified:** all 5 URLs 200 at 360–1920, no overflow, no console errors; `modal.js` only on the 3 detail pages.
- **Follow-up:** nav/footer link to out-of-scope pages → 404 until built/unlinked. Plan §12b. Next: Phase 4 ACF.

### 2026-09-16 session (opencode — LocalWP + Phase 1 inspection + homepage live)
- **LocalWP:** site `gerotech` (nginx, PHP 8.2.29, MySQL 8.4, WP 7.1) at `~/Local Sites/gerotech/app/public/` → http://gerotech.local. Parent theme pulled from `gerotechdev` via SFTP (avoided the portal "Pull" — it overwrites the dev environment). Child theme copied in + activated; homepage verified (desktop/mobile, 0 console errors).
- **Phase 1 findings:** parent `gerotech` (Tim Bomers/phiregroup, v2.o) is legacy, ~73MB, own `page-*.php` templates. No `wp_enqueue_style` (hardcodes CSS in `header.php` — overridden by child). `js_to_footer()` moves enqueues to the footer → child removes it at priority 1. Parent scripts `site-scripts`/`home_script`/`ts_script` dequeued. CPTs already registered (`case-study`, `training-session`, `testimonial`, `people`, `career`). ACF options page exists → reuse. Menus: `menu-primary`, `menu-mobile`, `menu-footer`, `menu-service`.
- `inc/enqueue.php` fixed accordingly. Parent notice (`functions.php:13`) quieted locally (`display_errors=0` in Local wp-config). Child-vs-new resolved: **child works**. Parent theme is **not** in the repo. Next: Phase 3 — remaining 9 pages.

### 2026-09-16 session (opencode — WP theme scaffold: header, footer, homepage)
- **New theme:** `wp-content/themes/gerotech-child/` — child of `gerotech` (parent TBC Phase 1). Phase 2 + homepage of Phase 3.
- **Files:** `style.css`, `functions.php`, `inc/helpers.php` (`gerotech_page_url()` slug→URL seam + `gerotech_quote_mailto()` + `gerotech_asset_version()`), `inc/enqueue.php`, `header.php`, `footer.php`, `front-page.php`, `template-parts/sections/testimonials.php`, `index.php`.
- **Assets copied in:** css (4), js (7 — `include-partials.js` dropped, `filter.js` not enqueued), images (22 + 2 gallery sets).
- **Assumptions:** nav + footer links hardcoded v1; internal links via `gerotech_page_url()` (URL strategy stays one seam); `Template: gerotech` + parent style handles filterable via `gerotech_parent_style_handles`; testimonials still static.
- **Not verified:** no local PHP — structural lint only; run `php -l` on staging. Prototype untouched.

### 2026-09-16 session (opencode — breakpoint check + top-level responsive type)
- **Breakpoint audit:** overflow scan across 10 widths (320–1920) × 10 pages — **no horizontal overflow anywhere**. Full-page screenshots at 390/768/1024/1440 confirmed layouts hold.
- **New fluid tokens:** `--fs-h4` (card/feature titles, `clamp(20px, 0.9vw+16px, 26px)`) + `--fs-stat` (stat numerals, `clamp(34px, 3vw+16px, 48px)`), with `--ls-h4` / `--lh-h4`.
- **Converted fixed → fluid:** `.category-card__title` (18), `.news-item__index`/`__title` (22), `.news-feature__stat-value` (26), `.email-signup__title` (24), `.testimonial-card__name` (20), `.mcs-card__title` (22), `.mcs-modal__title` (22), `.gallery-lightbox__title` (22), `.credential-band__badge-mark` (22), `.slide__ph-title` (20), `.haas-relationship__title` (24), `.stat-counter__value` (48). Removed the `.haas-relationship__title` 640px media override (fluid token handles it).
- Icons/brand marks (logo, hamburger, arrows, quote watermark, catalog numerals) intentionally left fixed. CSS brace-counts balanced.

### 2026-09-16 session (opencode — WP handoff file cleanup + handoff artifacts)
- **Housekeeping:** `.gitignore` now covers `.DS_Store` + `.codewhale/`; untracked/deleted `artifacts/` (QA scratch) and both tracked `.DS_Store` files.
- **Deleted unused legacy assets:** `gerotech-logo.png`, `haas-logo.png`, `haas-logo.svg`, `haas-service-distributor-award-2025.png`, `haas-umc-1500duo-ss.png`, `haas-umc-400-white-bg.png`, `haas-umc-400.png`, `haas-vm-3.png`, `testimonial-collaboration.png`, `testimonial-shop-floor.png`, `assets/js/testimonials.js` (dead script tag removed from `index-cta-lockup-preview.html`; `AGENTS.md` JS list updated).
- **New handoff artifacts** (plan §2): `handoff/theme-map.md`, `acf-spec.md`, `component-inventory.md`, `asset-manifest.md`, `js-spec.md`, `qa-checklist.md`.
- Stale `artifacts/homepage-hero-excel-5-slide.html` comments removed from `index.html` + `index-cta-lockup-preview.html`.
- **Not done (per user scope):** exploratory pages left in place (`hero-variations.html`, `showroom.html`, `index-cta-lockup-preview.html`, `gallery-module-preview.html` + `hero-showcase.css`); `v1.0-prototype` tag not created.

### 2026-09-16 session (Cursor — homepage `7196:2420` + lineup + peek slides)
- **Hero slide 1:** white eyebrow + rule; **Haas** `#CF0A2C`; **Explore the Haas Line** Haas-red CTA; no body; peek slider.
- **Hero slide 2:** showroom photo `hero-showroom.jpg`; “Our Showroom Machines Are For Sale! Ready To Ship”; warranty body; Browse Inventory → Haas HFO Gerotech showroom (new tab).
- **Hero slide 3:** `hero-automation-cell.jpg`; “Automation Built for Your Shop Floor”; body deleted; Explore Solutions.
- **Haas Relationship:** F1 card grey stroke; watermark vertically centered; eyebrow row `#CF0A2C`; lede is the long proud-to-serve paragraph.
- **Lineup — Machining Centers:** UMC-750 photo; 6 series + Haas links; View All Mills → vertical-mills.html.
- **Turning:** ST-25Y photo; ST / Box Way / Chucker URLs.
- **Rotaries / Automation / Tooling:** full panel copy; Cobots + Bar Feeders URLs; Tooling eyebrow + **Tooling & Workholding**; Winner’s Circle on **white** panel (`haas-winners-circle.png`).
- **Still open:** rotaries + Haas Automation tab photos still Unsplash; Design [WIP] remaining tab chrome if Tristien sends it; MCS video (140); gallery-module preview not promoted to live pages.

**Build direction:** static HTML → WordPress child theme + ACF, **no page builder**. Plan: `handoff/implementation-plan-wordpress-theme-acf.md`. Next WP: Phase 1 — inspect staging `gerotech` theme.

### 2026-09-16 session (Cursor — Design [WIP] 2 homepage comments)
- Applied Figma agent “done” items + remaining text/structure on `index.html` from comment list (hero Haas red, stats 39+/14,000+, Haas headline, remove capability band + news + catalog link + CTA tags/subtext, lineup tabs/links, CTA copy/button).
- Still blocked: attached images (149/165/166/179/191), full Navigo paragraphs not pasted (200, 198 Ford quote body), MCS video (140), ES older image comments already handled earlier.

**Build direction (chosen 2026-09-16):** static HTML → WordPress child theme + ACF fields, **no page builder**. Full build plan: `handoff/implementation-plan-wordpress-theme-acf.md`. PM-facing rationale: `handoff/gerotech-handoff-plan.docx`. Next action: Phase 1 — inspect staging `gerotech` theme, confirm child-vs-new, check CPT registration.

### 2026-09-16 session (opencode — handoff plan + WP implementation plan)
- **Deliverable 1:** `handoff/gerotech-handoff-plan.docx` — PM-facing Word doc (real tables via python-docx; `.html` kept as editable source). Covers current-site audit, theme strategy, builder comparison, 10 deliverables, scope, 6 phases, open decisions.
- **Deliverable 2:** `handoff/implementation-plan-wordpress-theme-acf.md` — build-facing plan for the **chosen approach: static HTML → WP child theme + ACF, no page builder.** Theme file tree, HTML→PHP map, ACF field model (incl. no-nested-repeaters / accent-word `em` / machine-lineup flat-repeater constraints), JS+asset port, forms, 9-phase sequence (~16–17 dev days), risks, open decisions.
- **Live-site audit (2026-09-16):** WordPress on **WP Engine** (old "Google Cloud" note wrong); custom `gerotech` theme; **no page builder** (homepage editor content empty — sections hardcoded in templates); plugins Megamenu, Smart Slider 3, CF7 + Honeypot, WPForms, iThemes Security, Search Regex; CPTs training/service/testimonials/people/careers; Google Font API.
- **Decision:** page builder rejected for v1 in favour of theme + ACF (fastest path; builder elements can be added later on retainer). If WP Bakery is revisited, cost estimate is ~46–70 dev days (24 custom elements + grid conflict + AJAX re-init + lock-in).
- **Still open:** child-vs-new theme (needs staging), nav editable vs hardcoded, URL strategy, CPTs-now-vs-ACF-only, accent-word editing. **Next:** Phase 1 — inspect staging `gerotech` theme.

### 2026-09-16 session (opencode — design-to-dev handoff plan for PM)
- **Deliverable:** `handoff/gerotech-handoff-plan.docx` — PM-facing Word doc (real tables via python-docx; `.html` kept as editable source). Covers current-site audit, theme strategy, builder comparison, 10 deliverables, scope, 6 phases, open decisions.
- **Live-site audit (2026-09-16):** WordPress on **WP Engine** (old "Google Cloud" note wrong); custom `gerotech` theme; **no page builder** (homepage editor content empty — sections hardcoded in templates); plugins Megamenu, Smart Slider 3, CF7 + Honeypot, WPForms, iThemes Security, Search Regex; CPTs training/service/testimonials/people/careers; Google Font API.
- **Recommendations (pending PM/client):** child theme of `gerotech` + CPTs extracted to a companion plugin (parent untouched → rollback); builder = **Gutenberg + ACF Blocks** (first) / **WP Bakery** (second); scope = **11 prototype pages only**; header/footer are theme-level, never builder-owned.
- **Still open:** builder choice, nav plugin vs rebuild, URL strategy (replace top-level vs new), machine-catalog links, FANUC/Haas lockup rights, news/social/signup, client photography. **Next:** Phase 1 (freeze prototype) + Phase 2 (inspect staging theme to settle child-vs-new).

### 2026-09-16 session (opencode — gallery module: collections + video)
- **Client request:** current flat one-photo-per-card gallery is wrong — they want **multiple images per category + a video option**. Built as an **isolated preview module** to share with the PM before rolling out to pages.
- **New (untracked, nothing on live pages touched):**
  - `gallery-module-preview.html` — exploratory preview page. Content mirrors MCS (7 categories from the current 13 flat cards). Header/footer partials + scripts included. Carries a "Preview module — not linked in the site nav" note.
  - `assets/css/gallery-module.css` — namespaced `.gallery-collection*` + `.gallery-viewer*`. Deliberately **self-contained** (does not reuse/override `.mcs-gallery-*` / `.gallery-lightbox`). Responsive ladder 4→3→2→1.
  - `assets/js/gallery-module.js` — parses `<template class="gallery-collection__data">` per card; derives count badges + `--has-video` class; builds a viewer scoped to the opened collection; supports images **and** `<video controls playsinline>`; keyboard (Esc/←/→/Tab trap), swipe, focus restore.
  - `assets/videos/placeholder-*.mp4` — ffmpeg-generated sample clips, clearly commented as placeholders awaiting client footage.
- **Data model:** each `.gallery-collection` card = one category; a `<template>` child lists its media as `[data-type="image|video"][data-src][data-poster][data-alt][data-caption]`. Badges/counts are derived, so authors only edit markup (maps cleanly to a WP ACF repeater later).
- **Card a11y:** heading holds a real `<button class="gallery-collection__trigger">`; `::after` stretches it over the card (valid HTML — no block content inside a button).
- **Verified:** chrome-headless-shell screenshots at 1440 + 390; image viewer (title/caption/`2 / 4`/active thumb), video viewer (poster + native controls + play badge on thumb), mobile 1-col. Live pages untouched.
- **Not done / next:** PM review → on approval promote module and convert MCS (13 cards → 7 collections), then Automation + Applications. No `href`/nav entry added anywhere.

### 2026-09-15 session (nav link trim)
- `partials/site-header.html`: Training / Support / About retain labels but lose `href` (non-links). Support + About `has-dropdown` classes, arrows, and dropdown menus removed. Desktop + mobile nav. Machines + Engineered Solutions stay linked. Get a Quote CTA and search-modal quick links left as-is (confirm if those should also unlink).


**Last Updated:** 2026-08-17 (Cline — responsive audit, universal spacing, Phases 1–7)
**Active Agent:** Cline
**Branch:** `master`
**Last Commit:** 5b7e7fe (Phases 1–3 responsive spacing, incl. prior Cursor WIP) — pushed
**Uncommitted:** Cline Phases 4–7 — `layout.css` + `components.css` (grid-step ladder, mobile hero alignment, legacy cleanup, header collapse ≥1120, grid `min-width:0`, btn wrap)

### 2026-08-17 session (Cline — responsive audit, Phases 4–7)
- **Phase 4 (grid consistency):** shared breakpoint ladder — `.grid-4` 4→2→1 and `.grid-6`/`.partners-grid` 6→3→1 (tablet step at 1024, single column at 640); `.grid-2` collapse moved 480→640. `.haas-relationship__grid` now 4-col → 2×2 with cross dividers (≤1024) → 1-col (≤640). Grid items get `min-width: 0` so images/long text can't force overflow.
- **Phase 5 (mobile hero alignment):** `.slide__content--left` + `.page-hero__actions` stay left-aligned at ≤768 site-wide (matches homepage peek), removing the interior-only mobile center override.
- **Phase 6 (legacy cleanup):** removed orphaned `.machine-browse` and `.machine-cards`/`.machine-card` blocks; dropped `.machine-browse` from the layout section list. (trust-strip/testimonial-split/carousel kept — cross-file, lower value.)
- **Phase 7 (Playwright verification):** installed Playwright in `/tmp` (repo kept clean). Programmatic scan of all 10 pages × 360/768/1024/1440 → **no horizontal overflow anywhere**. Fixes surfaced by the pass: header collapse breakpoint 1000→1120 (desktop nav overflowed at 1001–1119), about-page `.about-photo` grid overflow fixed via `min-width:0`, long `.btn` labels now wrap at ≤480. CSS brace-counts balanced; all pages serve 200.
- **Files:** `layout.css`, `components.css` (uncommitted).

### 2026-08-17 session (Cline — responsive audit · universal spacing, Phases 1–3)
- **Full static responsive audit** — found "weird layouts" rooted in: (1) double horizontal gutter (48px vs 24px) where a `.container` is nested inside a padded section class; (2) mixed container widths per page (1200 vs 1120); (3) hardcoded `--sp-96` block padding on full-bleed bands ignoring the `--section-y` token.
- **One gutter source:** removed `padding-inline: var(--sp-24)` from the `layout.css` section list; `.container` is now the single horizontal-gutter provider. Added `padding-inline: var(--sp-24)` to the `__inner` wrappers that don't use `.container` (why/es-grid/tech-partners/capability/credential/trust/email-signup). Fixed the 48px double-gutter bug on news, testimonials, mcs-grid (application/automation/MCS), and `.machine-lineup`.
- **One container width:** `--max-es` 1120 → 1200 (unified to `--max-home`) so hero / stat-counter / body left edges align on interior pages.
- **Token-driven vertical rhythm:** added `--section-y-lg: clamp(72px, 8.5vw, 120px)`; `.machine-lineup`, `.haas-relationship__top`, `.cta-band--cinema-lockup .cta-band__content` now use it (removed their hardcoded `--sp-96`/`--sp-64` + the fixed media overrides).
- **Files:** `tokens.css`, `layout.css`, `components.css`. CSS brace-counts verified balanced.
- **Remaining (not done):** Phase 4 breakpoint-ladder + grid-step consistency (grid-4/6, partners, haas features jump straight to 1-col), Phase 5 mobile hero alignment (homepage peek stays left, interior heroes center), Phase 6 legacy cleanup, Phase 7 Playwright screenshot pass at 360/768/1024/1440.
- **Note:** working tree already had uncommitted Cursor WIP (modal.js, several HTML, docs) — left untouched, not committed.

### 2026-08-17 session (Cursor — header responsive layout)
- **Root cause:** `.site-header__inner` lacked `width: 100%` — on mobile the flex row shrink-wrapped (logo + hamburger) and centered in the viewport, so logo no longer aligned with `.container` left inset.
- **Fix:** `width: 100%` on header + alert inners; alert bar horizontal padding unified to `var(--sp-24)` on inner (was 16px on banner). At ≤1000px hide desktop `<nav>` wrapper; hamburger gets `margin-left: auto`. ≤768px tighter header row + centered alert items; ≤480px slightly smaller logo.
- **Files:** `components.css`, `layout.css`.

### 2026-08-17 session (Cursor — header logo padding)
- **`.site-header__inner`:** Added `padding-inline: var(--sp-24)` (was vertical-only) so logo aligns with `.container` / section rhythm; logo no longer flush to viewport left edge.

### 2026-08-17 session (Cursor — ES detail UX)
- **Figma `7009:58`:** Mobile contact bar (black/white/orange) — not a lightbox. Close + lightbox chrome uses that high-contrast treatment.
- **`.mcs-modal__close`:** Ink disc, white X, white ring + shadow; orange hover. Top scrim on `.mcs-modal__img`. MCS / Application / Automation.
- **Gallery lightbox:** `.mcs-gallery-card` opens `.gallery-lightbox` (arrows, keys, swipe, thumb scroller) via `modal.js`.

### 2026-08-17 session (Cursor — continued)
- **Testimonials partial:** `partials/testimonials-block.html` — `.accent--deep` on "Michigan" + `.headline-rule--deep` under title (matches news section header pattern site-wide).
- **Email signup unified:** All 9 main pages now use homepage copy — `Join Our <span class="accent">Mailing List</span>` + shared subcopy. Added missing `.email-signup` to Application + MCS (`email-input-app` / `email-input-mcs`).
- **JS cleanup:** Removed dead `testimonials.js` from `index.html` + `engineered-solutions.html`; dropped stale `.testimonial-split` from `animations.js` scroll targets.

### 2026-08-17 session (Cline)
- **Section-header accent system site-wide:** Added `.accent--deep` accent words + `.headline-rule--deep` under every `section-title` on ES, MCS, Automation, Application, Training, Support, About, Careers (was homepage-only). Light sections use `.accent--deep` / `.headline-rule--deep`.
- **ES news → editorial split:** Engineered Solutions "Latest Projects & News" converted from legacy `.news-section--split`/`.news-card` to the homepage's `.news-section--editorial` / `.news-editorial` (photo lead + numbered rows 02–04). No orphaned legacy classes.
- **Stat counters added:** MCS, Automation, Application now include the `.stat-counter` band (37+/4,000+/12/#1) after their hero + wired `stat-counter.js`. About/Careers keep `.page-hero-trust` (existing decision); Training/Support unchanged.
- **Testimonials site-wide:** shared `partials/testimonials-block.html` included before the CTA band on MCS, Automation, Application, Training, Support, About, Careers (was homepage + ES only).
- **Verified:** all 9 pages serve 200; all edited HTML tag-balanced (err=0); editorial/news CSS classes present.

### 2026-08-17 session (Cursor)
- **Haas features band invert:** 7080:2240 grid → white bg, dark ink titles, gray body/labels, black icons on `--clr-gray-card` tiles; hairline dividers `rgba(0,0,0,0.08)`.
- **News redesign:** Homepage "Latest Projects & News" → `.news-editorial` split (dark photo lead `.news-feature` with stat row + numbered `.news-item` rows 02–04). Old `.news-card` styles kept for ES/showroom/preview pages. Story links still pending news page.
- **Testimonial refresh:** `.testimonial-card` — orange top rule (widens on hover), serif quote watermark bottom-right, hairline divider + Barlow Condensed name + uppercase micro role label. CSS-only; shared partial untouched so Homepage + ES stay in sync.
- **Haas Relationship features band:** Figma 7080:2240 — replaced 2×2 light grid + hr divider with full-bleed dark `.haas-relationship__features` (4 columns, vertical dividers, stacked icon/label/title/body); white icons via CSS filter on existing `haas-rel-*` SVGs; CTAs remain in white band below.
- **Haas Relationship watermark:** Figma 7080:1405 — `haas-wordmark-watermark.svg` at 5% opacity, `position: absolute` inside `.haas-relationship__intro` behind copy/brand (z-index 0/1); Figma left bleed ~-402px; removed parallax `haas-scroll-bg.js`; headline line breaks + split accent per Figma.
- **Lockup CTA (site-wide):** `.cta-band--cinema-lockup` (copy-left + call card) added to components.css; applied to all 9 pages with per-page copy. Careers / Support / Training keep their own actions (no call card — full-width copy).
- **Accent system:** `.accent` / `.accent--deep` / `.headline-rule` in components.css; orange accent words in hero + section headlines site-wide (bright orange on dark, deep orange on light).
- **Testimonials:** shared partial converted from split-photo carousel to `.testimonial-card` grid (`.testimonial-grid` in layout.css); live on Homepage + Engineered Solutions.
- **Contrast fixes:** added `--clr-orange-deep` token; `.btn-get-quote`/`.btn--primary` now ink-on-orange; `.btn--outline-orange`, eyebrows, and light-surface accent text use deep orange.
- **A11y:** search modal traps focus (Tab/Shift+Tab) and makes background inert while open.
- **CTA band:** homepage cinema CTA → 1200px, left-aligned text.
- **ES stat counter:** replaced old `.stat-strip`/`.trust-strip` with homepage `.stat-counter` (37+/4,000+/12/#1).
- **Decision:** About + Careers hero trust facts kept as-is (not converted to counter).
- **Preview:** `index-cta-lockup-preview.html` — exploratory lockup variant, committed.

### Current headline font: Barlow Condensed (Google Fonts)
- **Display font:** `--font-display: 'Barlow Condensed'` — headlines only (hero, section titles, card titles, stat counter, etc.)
- **Body/UI font:** `--font-sans: 'navigo'` — unchanged (Adobe Fonts kit `lqh7ybe`)
- **Google Fonts link** added to all 12 HTML files (`Barlow+Condensed:wght@500;600;700`)
- **Display weight:** 600 (semi-bold) via override rule in components.css; hero headline exception at 700 (bold)
- **Display scale:** bumped ~25-28% (Barlow Condensed is narrower); `--fs-hero: clamp(40px, 8vw, 106px)`, `--fs-h1: clamp(34px, 4.5vw, 64px)`, `--fs-h2: clamp(30px, 2.6vw + 12px, 50px)`
- **Tracking/leading:** loosened tracking, tightened leading for condensed face
- **Hero text:** eyebrow "Gerotech", headline "The Haas Outlet for Michigan." (106px bold)
- **Header:** L/R padding removed; hero content container 1200px centered (`--max-home`)
- **Homepage:** Figma redesign (hero/intro/machine cards/CTA/email signup) + stat counter (37+, 4,000+, 12, #1) + FANUC ASI mega-menu + top bar + footer updates
- **Haas Relationship (2026-08-17):** Figma `7080:1405`/`7080:2240` — static watermark behind intro (`.haas-relationship__bg` / `__content` layering); inverted white features band with numbered `01 ·`–`04 ·` columns
- **News (2026-08-17):** Homepage + ES `.news-editorial` split (`.news-feature` lead + numbered `.news-item` rows); `.news-card` legacy on showroom/preview only
- **Testimonials (2026-08-17):** Shared partial → `.testimonial-grid` + refreshed `.testimonial-card`; partial headline uses `.accent--deep` + `.headline-rule--deep` — not carousel
- **Feature icon tiles (2026-08-17):** `.category-card__icon` + `.why-feature__icon` grouped with `.haas-relationship__icon` — 40×40 gray card, 4px radius, 18px ink icons (not orange-tint tiles)

## Current State (2026-07-14)
- **Prototype stage:** Presentation-ready on `master` — beauty pass, elevation layer, design fixes, hero unification complete
- **Hero system:** Homepage `.hero-slider` + all interior pages use unified `.page-hero` (same markup as homepage slide: `slide__bg`, `slide__overlay--left`, `slide__content--left`)
- **CTA bands:** `.cta-band--photo` matches hero (left-aligned, per-page Unsplash bg)
- **Homepage:** Trust strip replaced by stat counter + Haas Relationship; machine lineup tabs; testimonial card grid; editorial news split
- **ES hub:** Tech partners wordmarks, capability cards (01–03), stats/trust FAQ/testimonials filled
- **UX:** Search modal, email signup thanks state, sticky header blur, footer links wired
- **Orphan machine-modification.html:** ✅ Redirects to MCS
- **FANUC badge:** ⚠️ Styled placeholder — official logo pending client/legal
- **Mega-menu machine catalog links:** ⚠️ Still `#` pending Haas catalog URLs
- **Navigo Medium (500):** ❌ Recommended for kit `lqh7ybe` — tokens ready, not in kit yet
- **Unsplash URLs:** ⚠️ Verify periodically — replaced 404 IDs with `photo-1647427060118` (facility/training) and `photo-1716191299980` (automation/robotics); see `design-spec.md` → Image Placeholders

### Interior hero (CSS in `components.css` + `elevated.css`)

All interior pages: **`.page-hero`** — same structure as homepage hero slide. Per-page background via `<img class="slide__bg">` in HTML. ES detail pages (MCS, Application, Automation) include `.page-hero__breadcrumb`.

### What was completed 2026-07-14 (Cursor — interior hero unification)
- **All interior heroes** → left-aligned photo hero (same as homepage): ES, About, Support, Training, MCS, Application, Automation
- **MCS detail pages** → `.mcs-hero--photo` with dark treatment + breadcrumbs on photo bg
- **CSS:** `elevated.css` — shared overlay gradients, mobile center fallback

### What was completed 2026-07-14 (Cursor — CTA + image fixes)
- **CTA band** → left-aligned photo hero style (was centered dark block)
- **Broken Unsplash URLs** fixed on intro + CTA (404 images)
- Commits: `d7138d0`, `4331930`

### What was completed 2026-07-14 (Cursor — full design fixes pass)

- **Hero:** 3 slides max; H1 on slide 1 only; View Training → `training.html`.
- **ES:** Stats, trust FAQ, testimonials, FANUC award block styling, name-split filter tabs.
- **Naming:** Nav mega-menu + Automation page use `.mcs-name-split` pattern.
- **MCS:** Installed Projects gallery (distinct from service cards); modal copy complete.
- **CTAs:** Get a Quote → mailto; Talk to Engineer → tel; support/training wired.
- **UX:** Alert banner lighter + collapses on scroll; social icons hidden pending URLs.
- **machine-modification.html:** Canonical redirect to MCS.

### What was completed 2026-07-14 (Cursor — presentation-ready verification)

- **Branch:** `cursor/cline-beauty-pass` created from latest master; goals A–C were already landed via `adcd5a1` + `83ae812`.
- **Homepage:** Partners band (12 wordmarks), intro orange frame, Ken Burns hero, model-tag chips, polished testimonial quotes, mobile tab scroll — all present.
- **Engineered Solutions:** Emoji replaced with inline SVG stroke icons in `.why-feature__icon`.
- **Site-wide:** News-card hover, `--radius-card: 0`, per-page CTA photos — confirmed.
- **Screenshot QA:** Homepage, ES, MCS at 1440px; mobile machine tabs at 390px; HTTP 200 on all 9 pages.
- **Left untouched:** Nav label split, orphan page, FANUC badge, `#` CTAs.

### What was completed 2026-07-14 (Cursor — merge + CTA images)

- **Merge:** `cursor/beauty-pass-polish` fast-forwarded into `master`.
- **CTA bands:** Replaced the shared placeholder image with page-specific Unsplash stand-ins on index + all 8 interior pages (salvaged from `cursor/cline-beauty-pass` WIP; duplicate beauty-pass CSS/HTML from that branch was not committed).
- **Not committed:** `.claude/settings.json` (machine-local paths); Builder extension settings in `.vscode/settings.json` (npm dev server — project has no npm dev script).

### What was completed 2026-07-14 (Cursor — beauty pass)

- **Tokens & CSS:** `--radius-card`/`--radius-card-lg` → 0; `--clr-orange-tint`, `--clr-ink-tint`; intro image frame (orange offset border); partner wordmark grid; `.model-tags`/`.model-tag`; hero Ken Burns; news-card hover; machine-tab horizontal scroll; testimonial track shadow; consolidated duplicate machine-tab / why-feature SVG rules.
- **Homepage:** Intro image wrapped in `.intro-section__image-frame`; new partners section (12 wordmarks, no Takamaz); all 5 machine panels use model tags; testimonial slides 2–3 have polished draft quotes.
- **Engineered Solutions:** Emoji replaced with inline SVG stroke icons in `.why-feature__icon`.

### What was completed 2026-07-14 (Cursor — afternoon)

- **Cross-editor sync:** Added `.cursor/rules/`, `.vscode/` (dev server task, Figma MCP, extensions) + updated `AGENTS.md` (7080de6).
- **MCS naming split:** Hero, badge, breadcrumb, and section headings on `machine-custom-solutions.html` now visually separate "Machine" (lead, gray) from "Custom Solutions" (primary). New `.mcs-name-split*` classes in components.css (b992e93). Nav/mega-menu still "Machine Customization" until client confirms.

### What was completed 2026-07-14 (Claude Code — Homepage patterns propagated site-wide)

- **DECISION REVERSED (Matt, 2026-07-14):** interior pages no longer keep the white-card CTA. All 8 interior pages (about, support, application, automation-integration, engineered-solutions, machine-custom-solutions, machine-modification, training) now use the homepage's `cta-band--photo` treatment — **each page has its own Unsplash stand-in** (2026-07-14 merge); each page keeps its own eyebrow/headline/button (about + support keep their tel: links).
- Dark email-signup section added to about + support (the only pages missing it; unique input ids `email-input-ab` / `email-input-su`).
- Navigation needed no changes — header/footer are shared partials (`partials/site-header.html` already has the Machines mega dropdown) loaded on every page.
- showroom.html untouched (exploratory page with its own CTA experiment).
- Verified via Playwright at 1440px: CTA + signup on about/support/training/engineered-solutions/application, zero console errors.

### What was completed 2026-07-14 (Claude Code — Inline CSS migration)

- Removed index.html's entire inline `<style>` block: machine tab + panel styles now live at the bottom of components.css ("Machine Browse" sections), tokenized (`--fs-ui`, `--sp-*` where tokens exist).
- Removed ALL inline `style=""` attributes from index, about, support, engineered-solutions. New classes: `.slide__bg--right` (hero crop bias), `.news-more`, `.about-photo`, `.intro-section__image img` rule, and layout.css modifiers `.section--white`, `.section--gray`, `.es-hero--compact`, `.grid-2--split`, `.grid--offset-top`, `.section-body--spaced`.
- Deliberately left inline: showroom.html + hero-variations.html `<style>` blocks (exploratory variant pages, not yet promoted to the design system).
- Verified via Playwright screenshots (1440px): homepage tabs/panels, hero object-position, about, support — pixel-equivalent, zero console errors.

### What was completed 2026-07-14 (Claude Code — Machine tab polish)

- Fixed stray curved lines on inactive machine tabs (index.html inline styles): separators (`border-left` on `.machine-tab + .machine-tab`) were combined with `border-radius`, so each divider arced at top/bottom. Inactive tabs are now square (`border-radius: 0`) with straight dividers; only `.machine-tab.is-active` gets the radius (pill on mobile).
- Repaired broken CSS in components.css `.slide__content--left`: an earlier uncommitted edit left the old rule body orphaned after the selector was replaced, causing parse errors around line 2180. Removed the orphaned fragment.

### What was completed 2026-07-14 (Cline — Design iteration pass)

**Hero — Variant 3 (Left-Aligned Full-Bleed):**
- Converted from centered slider to left-aligned full-bleed with horizontal gradient overlay
- Content wraps to `max-width: var(--max-home)` matching other section containers
- All 6 slides have dummy content (CNC Machinery, Engineered Solutions, Service, Automation, About)
- Responsive fallback: centered + vertical gradient on mobile (≤768px)

**Machine Browse — Tabbed Panel System:**
- Replaced 4-column card grid with a tabbed panel layout (5 categories)
- **New 5th category: HAAS Tooling** (client request)
- Tabs: Machining Centers, Turning Centers, 5-Axis, Automation, HAAS Tooling
- Each tab reveals a split-image panel (large photo left, content + model list + CTA right)
- Tab container has no border or radius; only the active tab is rounded (`var(--radius-btn)`), inactive tabs square with straight dividers (updated by Claude Code, see above)
- JS tab switching (one panel visible at a time, aria-selected attributes)

**CTA Band:**
- Photo hero-style (`cta-band--photo`) with full-bleed image + dark overlay
- "Get Started" eyebrow above headline
- "Engage with us today" orange CTA button

**Email Signup:**
- Dark bg (`--clr-dark-nav`) with side-by-side layout (`email-signup__inner`/`email-signup__copy`)
- Title "Stay in the Loop" + subtitle on left, form on right

**Header refinements (carried forward from 07-13):**
- White bg, `#E4E4EA` bottom border, `0 2px 16px rgba(0,0,0,0.06)` shadow
- Navigo logo (SVG from Figma), dark text
- Nav carets changed to orange `+` plus signs
- Search icon: orange `--clr-orange` background with white SVG icon (exact Figma SVG)
- "Get a Quote" button: uppercase, 0.05em tracking

**Footer:**
- Solid `--clr-ink` background
- 4-column grid with SVG logo (white variant)
- Social icons (LinkedIn, Instagram, YouTube)
- Legal links in a flex container

**Typography (Excellent rating per UI/UX Pro Max review):**
- Font: Navigo via Adobe Fonts (kit `lqh7ybe`) — replaced Archivo + IBM Plex Sans
- 2-weight system (400 + 700 only): tokens correctly map 500→400, 600/800→700
- Fluid clamp type scale: `--fs-hero/h1/h2/h3` for headings, `--fs-lede/body/ui/caption` for text
- Line-height: 1.15 headings, 1.6 body
- Text-wrap: `balance` on all headings, `pretty` on body
- All CTA buttons use `text-transform: uppercase` + 0.05em letter-spacing

**Accessibility:**
- Global `:focus-visible` (orange 2px outline)
- Skip-to-content link in header partial
- `prefers-reduced-motion` (CSS + JS guards)
- Carousel dots: `aria-selected`, off-screen slides `aria-hidden` + links tabindex -1
- Hero LCP img: `loading="eager" fetchpriority="high"`

**Design language:**
- Card radius: 0 (squared edges on all cards — `.mcat-card`, `.service-card`, `.category-card`, `.stat-pill`, `.testimonial-card`, `.news-feature`)
- Section padding: 88px vertical
- Interior rhythm: 32px gap between machine headline and content
- Unified card hover: lift + shadow across all card types
- Image zoom-on-hover in card shells (disabled under `prefers-reduced-motion`)
- **Accent system:** `.accent` / `.accent--deep` in headlines; `.headline-rule` (48px orange bar) under section titles — same motif on testimonial card tops
- **Numbered editorial rows:** Barlow Condensed index numerals (`01 ·`, `02`–`04`) on Haas features band + news secondary items
- **Photo cards:** 105° left gradient + optional bottom scrim (hero, CTA, `.news-feature` lead)
- **Inverted light bands:** white bg + dark ink text (Haas features band `7080:2240`) — not dark-on-dark for multi-column capability grids
- **Haas watermark:** static, intro-only, layered via `.haas-relationship__bg` / `__content` — no parallax

**17 Unsplash stand-in images** placed across hero, intro, machine panels, testimonials, and news sections.

### Still blocked — do not guess
1. Training: own nav item vs. under Support dropdown
2. FANUC badge usage rights — badge is placeholder only
3. News "Show More" destination URL
4. Constant Contact API integration (WordPress concern)

### Awaiting client copy / assets
- Slides 2-6 copy (currently using strategic dummy content)
- Testimonial photos and quotes (client-provided)
- HAAS Tooling card image (currently reusing training photo)

---

## Who else is working on this codebase

**Cursor (Composer/Agent)** and **VS Code (Cline)** both edit this repo. Sync via git + this file + `JOURNAL.md` — see `AGENTS.md` → Session continuity.

Before ending any session: update **Current Session State** above and prepend to `JOURNAL.md`.

---

## Figma (remote MCP — available in every editor)

- **Server:** `https://mcp.figma.com/mcp` (remote MCP — no Figma desktop app, no local `:3845`)
- **File key:** `YgHwqyyFj57c1ZSbmfkL0c` (Gerotech-Design) · Haas intro `7080:1405` · Haas features `7080:2240` · ES wireframe `6217:425` · Homepage wireframe `6218:10`
- **Canonical doc:** `FIGMA.md` — read it for the full design-to-code flow and per-tool config
- **Config committed:** `.mcp.json` (generic), `.cursor/mcp.json` + `.cursor/rules/figma.mdc` (Cursor), `.vscode/mcp.json` (VS Code)
- **One-time per tool/machine:** connect Figma OAuth; enable **Share → MCP access** in the Figma file (one-time, shared)
- **Workflow:** paste a frame/layer link with `node-id` → `get_design_context` → `get_screenshot` → implement in this repo's HTML/BEM/tokens (not React/Tailwind verbatim)

---

## Project Context

- **Client:** Gerotech — Michigan CNC machinery distributor, Haas Factory Outlet, est. 1987
- **Agency:** CloudMellow (Matt)
- **This repo:** Static HTML/CSS/JS prototype (NOT the production build — final site will be WordPress)

---

## Tech Stack

- Pure HTML5 / CSS3 (custom properties) / vanilla JS (ES6) — zero frameworks
- Fonts: Barlow Condensed (Google Fonts) for display + Navigo (Adobe Fonts, kit `lqh7ybe`) — 400 + 700 for body/UI
- CSS load order: `assets/css/tokens.css` → `assets/css/components.css` → `assets/css/layout.css` → `assets/css/elevated.css`
- JS: `nav.js` (sticky, mobile, search modal, signup), `slider.js`, `filter.js`, `animations.js`, `machine-tabs.js`, `modal.js` (legacy `testimonials.js` removed — grid partial does not use carousel)

## Typography System (2026-08-17)

- **Display:** Barlow Condensed (`--font-display`, Google Fonts 500/600/700) — section titles, card titles, stat numerals, testimonial names, news index numerals
- **Body/UI:** Navigo (`--font-sans`, Adobe kit `lqh7ybe`) — paragraphs, labels, buttons, eyebrows
- **The Adobe Fonts kit serves Navigo 400 + 700** (plus italics). Tokens map `--fw-medium` / `--fw-semibold` to **500** — add Medium to kit for UI labels; until then 500 falls back to 400. Do not use 600/800 unless added to kit.
- Type scale lives in tokens.css: `--fs-hero/display/h1/h2/h3/h4/stat` (fluid clamps — do NOT re-override sizes in media queries) + `--fs-lede/body/ui/caption`. Tracking: `--ls-hero…h4` (negative, tightens with size), `--ls-caps` 0.14em (eyebrows), `--ls-meta` 0.08em (card labels). Leading: `--lh-*`.
- All headlines use scale tokens + `text-wrap: balance`; body/quotes use `--lh-body` and `text-wrap: pretty`; intro body capped at 62ch measure.
- Buttons: `text-transform: uppercase` + 0.05em letter-spacing, `--fw-bold` (maps to Navigo 700)

## Key Brand Tokens
- Orange: `#F38A2C` (`--clr-orange`)
- Dark bg: `#1C1C21` (`--clr-dark-bg`)
- Dark nav/footer: `#3A3A40` (`--clr-dark-nav`)
- Body gray: `#6B6B76` (`--clr-gray-body`)
- Border: `#E4E4EA` (`--clr-gray-border`)
- Max-width: 1200px (homepage) / 1120px (ES page)
- Radii: buttons 8px, cards 0 (squared), pills 99px

---

## What NOT To Do

- Client photography still pending — Unsplash stand-ins (each marked with an HTML comment) are approved placeholder treatment; swap them for client assets when confirmed. Do not present stand-ins as final imagery.
- Never guess at open client decisions — use placeholder + HTML comment
- Never use real FANUC logo — badge usage rights unconfirmed
- Never change CSS load order
- Never hardcode brand colors outside `tokens.css`
- Navigo Medium (500) recommended for kit `lqh7ybe` — tokens ready; 500 falls back to 400 until added
